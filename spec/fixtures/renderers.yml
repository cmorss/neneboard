# Read about fixtures at http://ar.rubyonrails.org/classes/Fixtures.html

rectangle.clean:
  stencil: rectangle
  style: clean
  script: "ctx.rect(x,y,width,height);"

rectangle.napkin:
  stencil: rectangle
  style: napkin
  script: "ctx.rect(x,y,width,height);"

circle.clean:
  stencil: circle
  style: clean
  script: "ctx.circle(x,y,radius);"

circle.napkin:
  stencil: circle
  style: napkin
  script: "ctx.circle(x,y,radius);ctx.circle(x + 2, y + 2,radius);"
